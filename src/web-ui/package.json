{
  "private": true,
  "scripts": {
    "lint": "eslint --ext .ts,.js,.vue --fix .",
    "start": "nuxt dev",
    "build": "nuxt generate",
    "postbuild": "touch .output/public/.nojekyll",
    "deploy": "gh-pages --dist .output/public --dotfiles",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.13",
    "@nuxtjs/eslint-config-typescript": "^11.0.0",
    "@nuxtjs/tailwindcss": "^6.1.3",
    "gh-pages": "^4.0.0"
  }
}
